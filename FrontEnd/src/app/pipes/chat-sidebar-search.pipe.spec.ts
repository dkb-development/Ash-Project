import { ChatSidebarSearchPipe } from './chat-sidebar-search.pipe';

describe('ChatSidebarSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ChatSidebarSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
