import { MyDateAgoPipePipe } from './my-date-ago-pipe.pipe';

describe('MyDateAgoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyDateAgoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
